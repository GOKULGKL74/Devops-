                linux (#)
#!/bin/bash
shortcuts:
move cursor to begining-ctrl+A
move cursor to end-ctrl+E
remove everthing -ctrl+U
remove everthing after cursor -ctrl+K
home  : cd   or  ~/
cd -  :frequently opened directry
ls -lstr  -use for list 
source .bashrc or source .bash_profile  -once u set env variable exceute this   

what is home directory?
/home/ubuntu    ubuntu ->username 
whoami -->username 

vi -editor  nano editor 
:wq  ->w-save q -quit
:q!   ->without save nd quit
cat -see content with out opening

recursive create folder -folder inside folder
  mkdir -p abc/test/demo

how to istall package?
  apt-get install package*      //sudo apt-get install vim   ->sudo -admin



Linux commands
->echo $PATH     //Check what are the environmental variable are there
->pwd             //directory path
->ls -a           //hidden folders
->ls -l           //list the details of the folder
->ls -R           //to check all folder inside folder in one cmd
->cd .            //current directry
->cd ..           //previous directry
->cd              //home 
-r   /recursive     eg: if file inside folder rm wont wrk so use -r // rm -r file2

man ls  //
  man -description of commnd
./ filenmae.sh   or sh filename.sh  //execute script

uname -a   ->details of os 

history  //previous commds
whoami 

creation:    >   -  output 
  cat > file.txt 
  cat > file2.txt
  cat file.txt file2.txt > output.txt     /merge all contents put in output.txt .if old txt it can be replaced
  cat file.txt file2.txt         //show contents 
  cat file.txt         //open file contents
  echo "hi" > file.txt     //old contents replaced by hi
   or 
echo "hi" >> file.txt    // >> append    
echo history >> file.txt 
  cat hello.txt | tr a-z A-Z >hi.txt      //content in hello.txt chnged to capital ns stored in hi.txt  tr-translate 

 

copy: cp file.txt file2.txt
      cp -R file file2     /copy folder use -R even has multiple directories
move:mv file.txt ./name     next directry
    mv file.txt  ../name    previous directry
      mv file.txt rename.txt   //rename
remove: 
  rm file.txt
  rm -rf file   //if permission err  f-folder
  rm *
  rm *.txt




head  /top 10 fine   
    head -n 4 file.txt     //first 4 lines 
tail   /bottom 10 lines
tail -f  //some process runing -f denotes last line output
more   /more lines
diff //compare 2 files  
  dif name.txt name2.txt
locate -search the file 
  locate " "
find -find in particular directry
  find ""
diff -differnce btween 2 files 
  diff txt.file txt2.file 

hardlink:
ln file.txt file2.txt   //file2 (updated name like alias )same data with two reference
soft link:
ln -s file.txt file2.tx   //just an refernce if u delete original it wont point so no data

Alias:
cd /abc/data/file
alias car="cd /abc/data/file"  [temporary if permenantly need p[aste in .bashrc file] 
>> car      //u r in /abc/data/file 

nohup -for background running   
nohup python3 file.txt >> output.log &    [clieck enter twice]
  verfify?
  top  -task manager
  ps   -process running
  ps -aux |grep file.txt

Permission:  chmod 3 arg -user(owner) grp everyone 
4-read
2-write
1-exec
  read(r) writr(w) exec(x)  -user sudo(grp) others
  chmod u=rwx, g=rx,o=r file.txt   //we can giv permission ch -change 
  chmod 777 file.txt    //all permission
  chmod 444 file.txt    //read permission

0-no permission  
  chmod 577 file.txt   //user have read nd ecex other,sudo have rwx

Root permission://adminstrative purpose
  1.change to sudo then give root permission
  sudo chown root file.txt

sudo -root user 
whoami 


................
process
  1.ps -ef   //p-process f-details of the process in full format
  2.ps -ef | grep gokul | awk '{print $1}'    /to get id 1-coulmn
Search 
  grep "hi" file.txt
  grep -w "hi f" file.txt    //w word if full word present will get output
search word in cureent directry  
  grep -win "gokul" ./*.txt  /w -word i -case sensitive n-line number
ping- to check connection established

zip -zip zipfile.zip file.txt      //zipfile.zip -zip filename file.txt -whch file going to zip
unzip -unzip file.txt

networking cmnds:
1. nslookup google.com   //what is?  
2. curl google.com     //see contents of page 

set:
1.set -x  //debug mode  -to c command in output
2.set -e  //exit when the eror occur [if pipe s there it not wrks so use set -o pipefail]
    set -e
    set -o pipefail
[set -exo pipefail] -shortcut 

logs:
curl path  //retrive log from url
wget path   //download the log file nd store in loc
  wget "link"

search:
sudo find / -name file     //sudo -root user else we get permsiion denied  / -all folder 

vm:

ssh username@ip
  //username =ubuntu 

location
which git
which docker

package:
wget package   //package --copy from google 


top - task manager -currently running
df -disk space
ssh -ssh ipaddress@hostname    
wget -download file   //wget https
curl -fetch data //curl http:..
zip - zip -r zipFileName.zip /foldername    
unzip -unzip zipFileName.zip
tar -tar -czvf tarFileName.tar /scripts    //vf -show wht we done cz -force compress
    -tar -zxcf tarFileName.tar 

df -h    //check disc* size
nproc   /no od cpu* running
free    //check memory*
  free -m   ..mb
  free -g   ..gb
top     //everything at one command about usage size mry
wc filename -get count 

find -> find ./ -name gokul     //  ./  ->from this folder to any folder

ssh nd scp  
ssh: -putty  -connection
scp -winscp  -copy file frm one server to another 
ssh username@hostname     //username -ubuntu or whoami   hostname - type he cmd   
or ssh username@ip


  
